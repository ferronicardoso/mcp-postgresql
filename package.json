{
  "name": "mcp-postgresql",
  "version": "1.0.0",
  "description": "MCP server for PostgreSQL interaction via natural language",
  "type": "module",
  "bin": {
    "mcp-postgresql": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "hooks:install": "husky",
    "precommit:build": "npm run build && git add dist",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.11",
    "husky": "^9.1.7",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=18"
  }
}
